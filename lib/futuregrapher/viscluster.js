define(function(require) {

    var VisCluster = function (id, data, isCollapsed) {
        this.id = id;
        this.data = data;
        this.isCollapsed = isCollapsed;
    };

    return VisCluster;
});
