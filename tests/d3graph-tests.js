
Tinytest.add('d3graph tests - constructor test', function (test) {
    // Setup
    var el = $("<div />");
    var options = {};
    
    // Execute
    var graph = new d3graph(el, options);
    
    // Verify
});

